dist/
node_modules/
.next/
.turbo/
coverage/
pnpm-lock.yaml
.pnpm-store/

# Markdown is written by hand and by agents, and prettier pads table cells to
# align them. One word changing width then rewrites every row of the table, so
# diffs stop showing what actually changed.
*.md

# Written by drizzle-kit and read by drizzle-kit. Formatting them would put the
# working tree at odds with what the next `db:generate` writes back.
packages/db/drizzle/

# Vendored from the shadcn/ui skill repo. skills-lock.json records a hash of
# each file's original content; reformatting them would make every future
# `skills update` show a spurious diff against the upstream source.
.claude/skills/
